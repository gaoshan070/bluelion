server:
  port: 8501

logging:
  level:
    org.springframework.cloud: INFO
    com.bluelion: INFO
  file: /data/log/bluelion/usercenter/user.log
spring:
  profiles:
    active: dev
mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.bluelion.shared.model
