spring:
  application:
    name: bluelion-usercenter
  cloud:
    consul:
      discovery:
        prefer-ip-address: true
        health-check-path: /health
        health-check-interval: 15s
#      host: consul
      host: localhost
      port: 8500